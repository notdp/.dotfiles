# notdp/.dotfiles

- 上游仓库：`https://github.com/notdp/.dotfiles`
- 本地路径：`/Users/zhenninglang/.dotfiles/refs/notdp/.dotfiles`
- 主分类：**Agent 配置管理 / 多 Agent 工具链**
- 能力标签：`跨 Agent 统一配置`, `多 Agent 协作`, `工具增强`, `前端开发`, `自动化`
- 一句话总结：统一管理 33+ AI 编码 Agent 的 skills、commands 和全局指令的 dotfiles 框架，通过 symlink 实现"编辑一处，全部生效"。

## 能力概览

- 跨 Agent 统一配置：通过 symlink 将 ~/.dotfiles/ 映射到 33+ Agent 的配置目录。
- 交互式安装器：支持新建/导入两种模式，覆盖 Claude Code、Codex、Droid、Cursor、Windsurf 等。
- Skill 自动同步：通过 sync-sources.json + GitHub Actions 每日从上游仓库拉取最新 skill。
- 双 Agent 交叉 PR 审查（cross-review）：Opus + GPT 并行审查 → 共识判断 → 交叉确认 → 修复验证。
- Hive 通信层：tmux 内多 Agent 协作，支持消息注入、状态发布、artifact 传递。
- Droid 二进制修改（10 个 mod）：禁用输出截断、自定义模型切换、完整 effort 级别等。
- React/Next.js 性能优化规则：50+ 条 Vercel 工程团队最佳实践。
- 前端辅助：shadcn-ui、react-doctor、frontend-design 等。

## 资产盘点

- 12 个 skills（含归档 2 个）。
- 7 个 commands：ec、eh、learn、simplify 等。
- 8+ 个 scripts：安装器、同步、codex-patch 等。
- 4 个 GitHub workflows：skill 同步、PR 审查自动化。
- 全局 Agent 指令：agents/AGENTS.md。

## 关键文件

- `README.md`
- `agents/AGENTS.md`
- `skills/sync-sources.json`
- `skills/cross-review/SKILL.md`
- `skills/hive/SKILL.md`
- `skills/droid-bin-mod/SKILL.md`
- `skills/react-best-practices/rules/`
- `codex-patch/`
- `.github/workflows/sync-skills.yml`

## 备注

- Hive 和 Cross-Review 依赖外部 CLI 工具，非自包含。
- droid-bin-mod 和 codex-patch 与上游版本耦合，需跟踪更新。
- .arch/ 为归档内容（旧版 duoduo、chrome-devtools-mock）。
- absorption level：**high relevance / near-mirror**——与本仓库最接近的同类 dotfiles（stow + skills + agents/AGENTS.md + statusline + skill-lock），适合直接对照吸收。
- status：**已建档，跟踪中**。

## 2026-05-29 本地 range 调研

> 信号来源：本地 remote-tracking（未联网）。本轮 range `3b6976b5112a..d7ccc8462573`，`git log` 核实 5 commits。`git diff --stat` 触及 7 个文件：两份 README、`agents/AGENTS.md`、`config/ghostty/.config/ghostty/config`、`config/tmux/.tmux.conf`、`skills/.skill-lock.json`、`statusline.sh`。

- [事实] `fc15aab` chore: 把 `~/.tmux.conf` 和 `~/.config/ghostty/config` 纳入 stow 包，home 路径变为回指本仓库的符号链接，使配置编辑进入 git 历史；README 增加 stow 工作流，并说明 skills 改由独立 `skills` npm CLI 管理（per-skill fanout from `~/.agents/skills/<name>`），不再走集成 installer。
- [事实] `200a9da` docs(agents): `agents/AGENTS.md` 新增 HTML report 输出规则——约束 agent 何时渲染 HTML report vs 纯 chat 回复，渲染时要求自包含 CSS 与清晰视觉层级，并在 chat 给出简短中文摘要 + 本地文件路径。
- [事实] `b529142` chore(skills): bump `skills/.skill-lock.json` 到最新上游 hash。
- [事实] `373d5d7` fix(statusline): bash 5.3 不再匹配 `${var/$HOME/~}`（`$HOME` 含斜杠时整路径泄漏到状态栏），改用带显式 guard 的前缀剥离 `${var#$HOME}` 做 home 缩写。
- [事实] `d7ccc84` chore: 把 tmux + ghostty stow 包收进 `config/`，README 改用 `stow -d config -t ~`（stow dir 的父目录不再是 `$HOME`）。
- [推断] AGENTS.md 的 HTML report 输出规则与本仓库 `readable-html-artifact` skill 同域（“大报告才出独立 .html、chat 给中文摘要 + 路径”策略一致），是直接可对照的参考点。

## 最近 14 天更新（2026-03-31 ~ 2026-04-14）
<!-- recent-updates:start -->
- [事实] 检查基线：`origin/main`
- [事实] 提交数：`17`
- [事实] 代表提交：
  - `2026-04-08` `feat(droid-bin-mod): add unicode partial-json fixes`
  - `2026-04-08` `feat(droid-bin-mod): add mod10 fix ym9 tag strip truncation bug (0 bytes)`
  - `2026-04-14` `chore(sync): refresh mirrored skills`
  - `2026-04-08` `chore(sync): add lark-* skills`
- [推断] `droid-bin-mod` 新增 unicode partial-json 修复与 ym9 tag strip truncation 修复，同时继续刷新镜像 skills，并补入 `lark-*`、`hfork`/`vfork`/`notify` 等能力。
<!-- recent-updates:end -->
